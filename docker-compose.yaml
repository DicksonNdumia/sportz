version: "3.8"

services:
  api:
    container_name: backend-api

    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "5000:5000"

    environment:
      NODE_ENV: development

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev

    restart: unless-stopped
