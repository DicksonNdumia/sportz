apiVersion: apps/v1
kind: Deployment
metadata:
  name: socketstwo-api
  labels:
    app: socketstwo-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: socketstwo-api
  template:
    metadata:
      labels:
        app: socketstwo-api
    spec:
      containers:
        - name: socketstwo-api
          image: dicksonndumia19/socketstwo-api:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthy
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 20
